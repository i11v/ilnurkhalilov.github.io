/**
 * Links
 */

.links
  @extend .container

  &__menu
    inline-block()
    margin 0 30px 0 0
    padding 0
    vertical-align top
    list-style none outside none

    &-link
      font 400 0.8em $baseFontFamily
      light-link(lighten($linkColor, 20%))
